---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: miweb-ingress
  namespace: miweb-apache
  labels:
    app: miweb-apache
  annotations:
      haproxy.org/path-rewrite: "/"
spec:
  rules:
  - host: foo.bar
    http:
      paths:
      - path: /webapp
        pathType: "Prefix"
        backend:
          service:
            name: miweb-service
            port:
              number: 80
