# creamos el servicio 
apiVersion: v1
kind: Service
metadata:
    name: miweb-service
    namespace: miweb-apache
spec:
    selector:
      app: miweb-apache
    ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
